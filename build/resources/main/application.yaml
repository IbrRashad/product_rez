
server:
  port: 8085

spring:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  application:
    name: ProductRez
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: Mirferid121

